import styled from 'styled-components';

export default function Logo(){
    return(
        <LogoContainer>
            RepoProvas
        </LogoContainer>
    )
}

const LogoContainer = styled.div `
    color:#FFF;
    font-family:'Monoton';
    font-size: 80px;
    margin-bottom: 100px;
`;
